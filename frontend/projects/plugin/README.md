Angular library for the IMAP mail plugin: the admin UI for configuring a mailbox and for
the `receive-mail` process link.

Published as `@valtimo-plugins/imap-mail-plugin`. See the
[plugin documentation](../../../documentation/plugin.md) for what the fields mean.
